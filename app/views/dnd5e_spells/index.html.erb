</script>
<div id="sort">
	
	<div id="classes">
		 <label>[</label>        
                        <%= link_to "Clear", "5th_edition_spell_index", {:style => "color: black;" } %>
        	<% Dnd5eClass.all.each do |classs| %>
                	<% if classs.id == @class_highlight %>
                        	<%= link_to classs.name, { :controller => "dnd5e_spells", :action => "index", :class => classs.id }, {:style => "color: gray;" }%>
                	<% else %>
                        	<%= link_to classs.name, { :controller => "dnd5e_spells", :action => "index", :class => classs.id }, {:style => "color: black;" } %>
                	<% end %>
        	<% end %>       
        	<label>]</label>     
	</div>

	<% if @class_highlight%> 
		<div id="archetypes">
			<label>[</label>        
        		<% @archetypes.each do |a| %>
                		<% if a.id == @archetype_highlight %>
                        		<%= link_to a.name, { :controller => "dnd5e_spells", :action => "index", :class => @class_highlight, :archetype => a.id}, {:style => "color: gray;" }%>
                		<% else %>
                        		<%= link_to a.name, { :controller => "dnd5e_spells", :action => "index", :class => @class_highlight, :archetype => a.id }, {:style => "color: black;" } %>
                		<% end %>
        		<% end %>       
        	<label>]</label>  
	<% end %>   
	</div>

</div>


<div id="search">
		<div id="searchwrapper">
		<%= form_tag( "5th_edition_spell_index" , :method => "get", class: "navbar-form", id: "search-form") do %>
      			<%= text_field_tag :search, params[:search], class: "span2", placeholder: "Search..." %>
		<% end %>
		</div>
</div>

<div id="index">

	<% @levels.each_with_index do |level, index| %>
		<h2><%= level %></h2> 
		<% @spells[index].each do |spell| %>
	        	<%=link_to  spell.name, dnd5e_spell_url(spell) %>
			<br>
		<% end %>
	<% end %>	

	<% if @spells.flatten.empty?   %>
		<h2> None </h2>
	<% else %>
		<div id=totop>
			<a href="5th_edition_spell_index"> to Top</a>
		</div>
	<% end %>
</div>
